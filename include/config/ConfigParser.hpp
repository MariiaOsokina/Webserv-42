#ifndef CONFIG_PARSER_HPP
#define CONFIG_PARSER_HPP

#include "ConfigStructs.hpp"

class Config {
    std::vector<ServerConfig> servers;
    // Method to parse the file and fill the vector

    //parse();
    //getConfig();
};

#endif